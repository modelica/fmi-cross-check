Run test.sil. Adapt parameters in Configuration tab.

This test was rejected because the _ref.csv contains too many variables.
The csv comparator provided by FMI community can not face this complexity.

The simulation runs in Silver.